<Window x:Class="GCA.UI.Views.PurchaseDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Purchase Details" Height="450" Width="650"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{Binding Id, StringFormat='Purchase #\{0\}'}" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="{Binding Date, StringFormat='Date: \{0:yyyy-MM-dd HH:mm\}'}"/>
            <TextBlock Text="{Binding Supplier.Name, StringFormat='Supplier: \{0\}'}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding LineItems}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Part" Binding="{Binding Part.Name}" Width="*"/>
                <DataGridTextColumn Header="Cost" Binding="{Binding UnitCost, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <TextBlock Text="Grand Total: " FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TotalCost, StringFormat=C}" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
        </StackPanel>
    </Grid>
</Window>
