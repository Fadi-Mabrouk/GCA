<UserControl x:Class="GCA.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:GCA.UI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:DashboardViewModel}"
             Padding="20">
    <UserControl.Resources>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,30">
            <TextBlock Text="📊 Business Dashboard" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="20,0,0,0" Padding="15,5"/>
        </StackPanel>

        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,30">
            <!-- Revenue Card -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Total Revenue" Foreground="Gray" FontSize="14"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=C}" FontSize="24" FontWeight="Bold" Foreground="#27AE60"/>
                </StackPanel>
            </Border>

            <!-- Purchase Cost Card -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Total Purchase Cost" Foreground="Gray" FontSize="14"/>
                    <TextBlock Text="{Binding TotalPurchaseCost, StringFormat=C}" FontSize="24" FontWeight="Bold" Foreground="#E67E22"/>
                </StackPanel>
            </Border>

            <!-- Net Profit Card -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Net Profit" Foreground="Gray" FontSize="14"/>
                    <TextBlock Text="{Binding NetProfit, StringFormat=C}" FontSize="24" FontWeight="Bold" Foreground="#2980B9"/>
                </StackPanel>
            </Border>

            <!-- Inventory Value Card -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Inventory Value" Foreground="Gray" FontSize="14"/>
                    <TextBlock Text="{Binding TotalInventoryValue, StringFormat=C}" FontSize="24" FontWeight="Bold" Foreground="#8E44AD"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2">
            <!-- continuation detail extra -->
            <TextBlock Text="..." Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center" FontStyle="Italic"/>
        </Grid>
    </Grid>
</UserControl>
