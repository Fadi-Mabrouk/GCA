<UserControl x:Class="GCA.UI.Views.PurchaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:GCA.UI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:PurchaseViewModel}"
             Padding="20">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: PART CATALOG (Same as Sales but for selection) -->
        <DockPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock DockPanel.Dock="Top" Text="Part Catalog (For Re-stock)" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Width="250" Padding="5">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Content="Search" Command="{Binding SearchCommand}" Margin="10,0,0,0" Padding="15,5"/>
            </StackPanel>

            <DataGrid ItemsSource="{Binding AvailableParts}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" BorderThickness="1" BorderBrush="Gray">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="100"/>
                    <DataGridTextColumn Header="Current Stock" Binding="{Binding Quantity}" Width="90"/>
                    <DataGridTemplateColumn Header="Action" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Select" Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Background="#3498DB" Foreground="White"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- RIGHT: PURCHASE ORDER CART -->
        <Border Grid.Column="1" Background="#ECF0F1" CornerRadius="5" Padding="15">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="New Purchase Order" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

                <!-- Supplier Selector -->
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,15">
                    <TextBlock Text="Select Supplier:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name" SelectedIndex="0" Padding="5"/>
                </StackPanel>

                <!-- PO Items -->
                <DataGrid DockPanel.Dock="Top" ItemsSource="{Binding Cart}" Height="300" AutoGenerateColumns="False" CanUserAddRows="False" Background="White">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Part" Binding="{Binding PartName}" Width="*" IsReadOnly="True"/>
                        <!-- Editable Qty and Cost -->
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="50"/>
                        <DataGridTextColumn Header="Unit Cost" Binding="{Binding UnitCost, UpdateSourceTrigger=PropertyChanged, StringFormat=C}" Width="80"/>
                        <DataGridTextColumn Header="Line Total" Binding="{Binding TotalCost, StringFormat=C}" Width="80" IsReadOnly="True"/>
                        
                        <DataGridTemplateColumn Header="" Width="40">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="X" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Background="Red" Foreground="White"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Total & Checkout -->
                <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                    <Separator Margin="0,10"/>
                    <DockPanel>
                        <TextBlock Text="TOTAL COST:" FontSize="24" FontWeight="Bold"/>
                        <TextBlock Text="{Binding GrandTotal, StringFormat=C}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </DockPanel>
                    
                    <Button Content="Confirm Purchase (Restock)" Command="{Binding CheckoutCommand}" 
                            FontSize="18" FontWeight="Bold" Background="#2980B9" Foreground="White" Padding="10" Margin="0,20,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
