<UserControl x:Class="GCA.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:GCA.UI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
             HorizontalAlignment="Center" VerticalAlignment="Center">
    <Border Background="White" Padding="30" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        <StackPanel Width="300">
            <TextBlock Text="GCA Login" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            
            <TextBlock Text="Username" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username}" Margin="0,0,0,15" Padding="5"/>
            
            <TextBlock Text="Password" Margin="0,0,0,5"/>
            <PasswordBox x:Name="pbPassword" Margin="0,0,0,15" Padding="5"/>
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,10"/>
            
            <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=pbPassword}" 
                    Padding="10,5" Background="#007BFF" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Border>
</UserControl>
